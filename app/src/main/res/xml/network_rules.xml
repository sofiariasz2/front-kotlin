<?xml version="1.0" encoding="utf-8"?>
<!-- El backend corre en HTTP dentro de la red local, así que se permite tráfico sin TLS. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
